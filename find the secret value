#include<stdio.h>
int main () {
    int secretValue = 19; // secret value intalization
    int userInput; // userInput decalaration
    scanf("%d", &userInput); // take userInput
    while(userInput != secretValue) // check the user input not equals to secret value
    {
     scanf("%d", &userInput); // take user input until condition is true
    }
    printf("You Won the secret value is %d",userInput); //user input equals to secret value
}
