#include<stdio.h>
int main () {
    int userInput;
    int sum = 0;
    do{
        scanf("%d", &userInput);
       if(userInput>0){
         sum = sum + userInput;
       
       }
      

    }
    while(userInput != 0);
 printf("%d", sum);
}
