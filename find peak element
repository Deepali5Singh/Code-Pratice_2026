#include<stdio.h>

int main () {
int arr[4] = {11, 2, 33,4};
low = 0;
high = n -1;
while(low<high){
int mid  = low + high /2.0 ;
if(arr[mid] > arr[mid+1])
low = mid + 1
else {
high  = mid;
}
printf(arr[low]);
}
}
